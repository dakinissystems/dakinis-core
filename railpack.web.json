{
  "$schema": "https://schema.railpack.com",
  "provider": "node",
  "packages": { "node": "20" },
  "steps": {
    "build": {
      "inputs": ["...", { "local": true, "include": ["."] }],
      "commands": ["npm run build -w @dakinis/web"],
      "deployOutputs": ["web/dist"]
    }
  },
  "deploy": {
    "startCommand": "npm run start:web"
  }
}
