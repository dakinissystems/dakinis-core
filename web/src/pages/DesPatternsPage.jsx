import { PatternsDemo } from "@dakinis/shared-ux";

/** Solo DEV — patrones DES B4. */
export default function DesPatternsPage() {
  return (
    <div className="modules" style={{ padding: "1.5rem 0 3rem" }}>
      <PatternsDemo />
    </div>
  );
}
