import { MotionElevationDemo } from "@dakinis/shared-ux";

/** Solo DEV — referencia visual DES B3. */
export default function DesMotionPage() {
  return (
    <div className="modules" style={{ padding: "1.5rem 0 3rem" }}>
      <MotionElevationDemo />
    </div>
  );
}
