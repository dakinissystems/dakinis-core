import { ThemeDemo } from "@dakinis/shared-ux";

/** Solo DEV — temas + HC QA (DES v1.1). */
export default function DesThemePage() {
  return (
    <div className="modules" style={{ padding: "1.5rem 0 3rem" }}>
      <ThemeDemo />
    </div>
  );
}
