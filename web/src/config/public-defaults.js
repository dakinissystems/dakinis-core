/**
 * Valores por defecto del cliente (solo desarrollo).
 * Debe coincidir con la maestra del servidor (`DAKINIS_MASTER_API_KEY` / `dakinis-dev-key`).
 */
export const DAKINIS_PUBLIC_DEFAULT_API_KEY = "dakinis-dev-key";
